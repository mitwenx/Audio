<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:background="@android:color/transparent">

    <!-- Icon representing the widget's function -->
    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_mic_widget" /* Default icon */
        android:scaleType="centerInside"
        android:background="?attr/selectableItemBackgroundBorderless" /* Ripple effect */
        android:contentDescription="@string/widget_cd_record" /* Default description */
        tools:src="@drawable/ic_mic_widget"/>

</FrameLayout>
